apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
        - name: admin-service
          image: kimngoc20/admin-service:latest
          env:
            - name: MONGODB_URI
              value: "mongodb://mongo:27017/auth_db"
 